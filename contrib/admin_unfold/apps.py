from django.apps import AppConfig


class OffermanAdminUnfoldConfig(AppConfig):
    name = "offerman.contrib.admin_unfold"
    label = "offerman_admin_unfold"
    verbose_name = "Offerman Admin (Unfold)"
